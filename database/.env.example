# Example database env for local development
# These values match the defaults used by docker-compose.yml
POSTGRES_USER=admin
POSTGRES_PASSWORD=eventura_pwd
POSTGRES_DB=eventura_db

# If you prefer, set a full connection string in the backend .env:
# DATABASE_URL=postgresql://admin:eventura_pwd@db:5432/eventura_db
